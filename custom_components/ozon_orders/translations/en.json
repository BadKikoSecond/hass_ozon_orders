{
  "config": {
    "step": {
      "user": {
        "title": "Ozon Orders",
        "description": "Paste JSON cookies from ozon.ru (Cookie-Editor / EditThisCookie export). {hint}",
        "data": {
          "cookies": "Cookies (JSON)"
        }
      }
    },
    "error": {
      "cannot_connect": "Failed to connect to Ozon",
      "invalid_auth": "Ozon rejected the session — re-export all ozon.ru cookies",
      "invalid_json": "Invalid JSON — paste the full cookie array",
      "missing_auth_cookies": "Missing __Secure-access-token — export all cookies",
      "antibot": "Ozon antibot: HA container blocked (not IP). Use a host sidecar"
    },
    "abort": {
      "already_configured": "Account already configured"
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Ozon Orders",
        "data": {
          "scan_interval": "Polling interval (minutes)"
        }
      }
    }
  },
  "entity": {
    "sensor": {
      "active_orders": { "name": "Active orders" },
      "at_pickup": { "name": "At pickup point" },
      "in_transit": { "name": "In transit" },
      "session_expires": { "name": "Session expires" },
      "order_status": { "name": "Order status" }
    },
    "binary_sensor": {
      "session_valid": { "name": "Session valid" },
      "order_at_pickup": { "name": "At pickup" },
      "order_in_transit": { "name": "In transit" }
    }
  },
  "services": {
    "refresh": {
      "name": "Refresh orders",
      "description": "Force refresh Ozon order data"
    }
  }
}
